# Components

Shared component references and component-system documentation for the PrecisionCore case study. The current production implementations remain inside each application until a shared package is extracted.
